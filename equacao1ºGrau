#include <stdio.h>

int main () {
    int a, x, b, n;
    printf("Digite a e b:");
    scanf("%d %d", &a, &b);
    if (a == 0) {
        printf("impossivel paizao.");
    }
    
    else {
        x = -b / a;
        printf("x = %d", x);
    }
}
