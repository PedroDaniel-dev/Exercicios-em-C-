#include <stdio.h>

int main()
{
    int m, n, maior, menor;
    scanf("%d %d", &m, &n);
    if (m - n > 0) {
        maior = m;
        menor = n;
    }
    
    else {
        maior = n;
        menor = m;
    }
    
    while (menor <= maior) {
        printf("%d\n", menor);
        menor++;
        
    }
    
    
}
